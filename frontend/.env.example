# For local dev: leave empty — Vite proxy forwards /api to localhost:8000
VITE_API_BASE=

# For production (Netlify / Railway frontend):
# Set this to your Railway backend URL, e.g.:
# VITE_API_BASE=https://findleaks-production.up.railway.app/api
